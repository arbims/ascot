{% extends 'base.html.twig' %}

{% block body %}
    <h1>Type_Notification</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_Notification.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_Notification.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_Notification.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('type_notification_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('type_notification_edit', { 'id': type_Notification.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
