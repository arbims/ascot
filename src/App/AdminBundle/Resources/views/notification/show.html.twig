{% extends 'base.html.twig' %}

{% block body %}
    <h1>Notification</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notification.id }}</td>
            </tr>
            <tr>
                <th>Vue</th>
                <td>{{ notification.vue }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ notification.lien }}</td>
            </tr>
            <tr>
                <th>Idemetteur</th>
                <td>{{ notification.idEmetteur }}</td>
            </tr>
            <tr>
                <th>Idtypenotif</th>
                <td>{{ notification.idTypeNotif }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('notification_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('notification_edit', { 'id': notification.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
