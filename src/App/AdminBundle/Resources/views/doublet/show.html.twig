{% extends 'base.html.twig' %}

{% block body %}
    <h1>Doublet</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ doublet.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ doublet.libelle }}</td>
            </tr>
            <tr>
                <th>Numjoueur</th>
                <td>{{ doublet.numJoueur }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('doublet_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('doublet_edit', { 'id': doublet.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
