{% extends 'base.html.twig' %}

{% block body %}
    <h1>Evaluation list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Note</th>
                <th>Commentaire</th>
                <th>Etat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evaluation in evaluations %}
            <tr>
                <td><a href="{{ path('evaluation_show', { 'id': evaluation.id }) }}">{{ evaluation.id }}</a></td>
                <td>{{ evaluation.note }}</td>
                <td>{{ evaluation.commentaire }}</td>
                <td>{{ evaluation.etat }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('evaluation_show', { 'id': evaluation.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('evaluation_edit', { 'id': evaluation.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('evaluation_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
